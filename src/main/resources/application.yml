server:
  port: 8081
  servlet:
    context-path: /api
spring:
  application:
    name: mall
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hahow_mall?serverTimezone=Asia/Taipei&characterEncoding=utf-8
    username: root
    password: root_password
  jpa:
    properties:
      hibernate:
        format_sql: false
    show-sql: true
  messages:
    basename: i18n/messages
logging:
  level:
    org:
      hibernate:
#        SQL: DEBUG
        type:
          descriptor:
            sql: TRACE